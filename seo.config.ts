// Global configuration for SEO Toolkit
import type { SEOOptions } from "./src/index";

const config: SEOOptions = {
	title: "Default Title",
	description: "Default Description",
	// ...other fields
};

export default config;
